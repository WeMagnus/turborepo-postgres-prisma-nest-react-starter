NODE_ENV=development

# Server-side (NOT exposed to the browser)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
PORT=3000

# Vite client-side (exposed in the browser bundle)
VITE_API_URL=http://localhost:${PORT}
VITE_PORT=5173
